


struct GIData {
    vec3 gridStart;
    vec3 gridEnd;
    int stackSizeX;
    int stackSizeY;
    int gridSize;
    vec3 voxelSize;
    vec3 gridScale;
    vec2 entrySize;
    vec3 normalizationFactor;
    sampler3D voxels;
    sampler3D geometry;
};

