

struct VertexOutput {
    vec3 positionWorld;
    vec3 normalWorld;
    vec2 texcoord;

    vec4 materialDiffuse;
    vec3 materialSpecular;
    float materialAmbient;

    // vec3 tangentWorld;
    // vec3 binormalWorld;

    vec4 lastProjectedPos;
};