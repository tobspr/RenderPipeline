#pragma once

struct TransparentMaterial {
    vec3 color;
    vec3 normal;
    float alpha;
    float depth;
    vec2 texcoord;
    float roughness;
    float specular;
    float metallic;
    uint nextEntry;
};