#version 130

#pragma optionNV (unroll all)

#include "Includes/Configuration.include"
#include "Includes/PostProcessBlur.include"

uniform sampler2D colorTex;
uniform sampler2D dofStorage;

in vec2 texcoord;

void main() {
    // gl_FragColor = mipmapBasedBlur(texcoord, vec2(0,1), colorTex, dofStorage);
    gl_FragColor = texture(colorTex, texcoord);
}